# 🚀 Руководство по деплою Telegram Tracker

## ✅ Предварительные требования

### 1. Переменные окружения
Установите в Vercel следующие переменные:

```env
ENCRYPTION_SECRET=ваш-секретный-ключ-минимум-32-символа
CRON_SECRET=секрет-для-cron-задач
```

### 2. Миграции базы данных
Выполните SQL-скрипты в Supabase в следующем порядке:
1. `scripts/004_create_telegram_sessions_table.sql`
2. `scripts/005_create_jobs_table.sql`
3. `scripts/006_add_channel_username_to_channels.sql`

## 🔄 Процесс деплоя

### Шаг 1: Деплой на Vercel
```bash
# Если используете Vercel CLI
vercel --prod

# Или через GitHub integration (автоматически)
```

### Шаг 2: Настройка Cron Jobs
В панели Vercel перейдите в Settings → Functions и включите Cron Jobs:
- Путь: `/api/cron/process-jobs`
- Расписание: `*/5 * * * *` (каждые 5 минут)

### Шаг 3: Настройка Telegram API
1. Перейдите на https://my.telegram.org и получите API ID и Hash
2. В приложении перейдите в Settings → Telegram API Configuration
3. Введите API ID и Hash, сохраните настройки

### Шаг 4: Аутентификация в Telegram
1. В Settings → Telegram Authentication нажмите "Start Authentication"
2. Введите номер телефона в международном формате (+1234567890)
3. Введите код верификации из Telegram
4. Если включена 2FA, введите пароль

## 🛠 Что изменилось

### ✅ Удаленные зависимости
- `cheerio` - больше не нужен для парсинга HTML
- `@erenodaci/telegram-message-scraper` - заменен на официальный API
- `telegram-scraper` - заменен на официальный API

### ✅ Новые возможности
- **Шифрование данных**: API ключи и сессии шифруются
- **Фоновая обработка**: Добавление каналов происходит асинхронно
- **Официальный API**: Используется настоящий Telegram API
- **Автоматическая обработка**: Cron job каждые 5 минут

### ✅ Новые файлы
```
lib/encryption.ts                    # Шифрование данных
lib/telegram-client.ts               # Работа с Telegram API
app/api/telegram/auth/               # Аутентификация
app/api/cron/process-jobs/route.ts   # Обработка задач
scripts/004_*.sql                    # Таблица сессий
scripts/005_*.sql                    # Таблица задач
scripts/006_*.sql                    # Добавление channel_username
vercel.json                          # Cron конфигурация
```

## 🔐 Безопасность

- **ENCRYPTION_SECRET**: Используется для шифрования API ключей и сессий
- **CRON_SECRET**: Защищает cron endpoint от несанкционированного доступа
- Все чувствительные данные хранятся в зашифрованном виде

## 🐛 Возможные проблемы

### Конфликты зависимостей
Vercel автоматически разрешит конфликты зависимостей (React 19 vs vaul).

### Cron Jobs не работают
Проверьте, что:
1. CRON_SECRET установлен
2. Cron job включен в Vercel dashboard
3. Путь `/api/cron/process-jobs` доступен

### Telegram API ошибки
1. Проверьте API ID и Hash
2. Убедитесь, что аутентификация прошла успешно
3. Проверьте лимиты API (flood control)

## ✅ Статус готовности
- ✅ Код готов к деплою
- ✅ Конфигурация Vercel настроена
- ✅ Безопасность реализована
- ✅ Асинхронная обработка готова

**Можно деплоить! 🚀**
